{
  "name": "tanish-mvp-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run",
    "login": "wrangler login",
    "types": "wrangler types",
    "check": "wrangler check startup"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "wrangler": "^4.0.0"
  }
}